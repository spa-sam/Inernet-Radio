{
  "identifier": "default",
  "description": "Default capabilities for the app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:menu:default",
    "core:tray:default",
    "dialog:default",
    "fs:default"
  ]
}
